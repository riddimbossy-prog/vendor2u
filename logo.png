<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vendor2U - African &amp; Multicultural Event Vendors</title>
    <link rel="icon" type="image/png" href="/favicon.png">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        // Vendor2U brand palette (pulled from the logo)
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        navy:   { DEFAULT: '#02244F', 700: '#062F63', 800: '#031B3D', 900: '#02132B' },
                        brand:  { DEFAULT: '#FC700E', 600: '#FC700E', 700: '#E25E00', 50: '#FFF3E9', 100: '#FFE4CC' }
                    }
                }
            }
        };
    </script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Playfair+Display:wght@700&display=swap');
        body { font-family: 'Inter', system-ui, sans-serif; }
        .heading-font { font-family: 'Playfair Display', sans-serif; }
        .hero-bg {
            background: linear-gradient(rgba(2,19,43,0.78), rgba(2,19,43,0.85)), url('https://picsum.photos/id/1015/2000/1200') center/cover;
        }
        @media (prefers-reduced-motion: reduce) { * { scroll-behavior: auto !important; } }
    </style>
</head>
<body class="bg-gray-50 text-gray-900">
    <!-- Navbar -->
    <nav class="bg-white border-b sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <div class="flex items-center gap-8">
                <div class="flex items-center gap-2.5 cursor-pointer" onclick="goHome()">
                    <div class="w-10 h-10 bg-navy rounded-xl flex items-center justify-center text-white text-lg font-extrabold tracking-tight">V<span class="text-brand">2</span>U</div>
                    <span class="text-2xl font-bold tracking-tight text-navy">Vendor<span class="text-brand">2</span>U</span>
                </div>
                <div class="hidden md:flex items-center gap-6 text-sm font-medium text-navy/80">
                    <a href="#" onclick="scrollToSearch(); return false;" class="hover:text-brand transition-colors">Find Vendors</a>
                    <a href="#" onclick="scrollToId('categories-section'); return false;" class="hover:text-brand transition-colors">Categories</a>
                    <a href="#" onclick="scrollToId('how-section'); return false;" class="hover:text-brand transition-colors">How it Works</a>
                </div>
            </div>
            <div class="flex items-center gap-4">
                <button onclick="toggleLoginModal()" class="px-5 py-2 text-sm font-medium text-navy/70 hover:text-navy transition-colors">Log in</button>
                <button onclick="showBecomeVendor()" class="px-6 py-2.5 bg-brand hover:bg-brand-700 text-white rounded-2xl text-sm font-semibold transition-all">Become a Vendor</button>
            </div>
        </div>
    </nav>

    <!-- Hero -->
    <header class="hero-bg text-white py-24">
        <div class="max-w-5xl mx-auto px-6 text-center">
            <h1 class="heading-font text-5xl md:text-7xl font-bold mb-6 leading-tight">
                Your Event.<br>Your Culture.<br>Your <span class="text-brand">Vendors.</span>
            </h1>
            <p class="text-xl md:text-2xl mb-10 max-w-2xl mx-auto text-gray-200">
                Connect with trusted African and multicultural vendors for weddings, funerals, birthdays &amp; more across the US.
            </p>
            <!-- Search Bar -->
            <div class="max-w-4xl mx-auto bg-white rounded-3xl p-2 shadow-2xl">
                <div class="flex flex-col md:flex-row gap-2">
                    <div class="flex-1 relative">
                        <div class="absolute left-5 top-4 text-gray-400"><i class="fa-solid fa-magnifying-glass"></i></div>
                        <input id="search-input" type="text" onkeydown="if(event.key==='Enter')performSearch()"
                               placeholder="What service? (Caterer, Photographer...)"
                               class="w-full pl-12 pr-4 py-4 bg-transparent focus:outline-none text-gray-900 placeholder:text-gray-500 rounded-3xl">
                    </div>
                    <div class="flex-1 relative md:border-l">
                        <div class="absolute left-5 top-4 text-gray-400"><i class="fa-solid fa-location-dot"></i></div>
                        <input id="location-input" type="text" onkeydown="if(event.key==='Enter')performSearch()"
                               placeholder="City or state"
                               class="w-full pl-12 pr-4 py-4 bg-transparent focus:outline-none text-gray-900 placeholder:text-gray-500 rounded-3xl">
                    </div>
                    <button onclick="performSearch()" class="md:w-auto w-full bg-brand hover:bg-brand-700 text-white px-10 py-4 rounded-3xl font-semibold flex items-center justify-center gap-2 transition-all">
                        <i class="fa-solid fa-search"></i><span>Find Vendors</span>
                    </button>
                </div>
            </div>
            <div class="mt-8 flex flex-wrap justify-center gap-4 text-sm">
                <div onclick="quickSearch('Catering')" class="cursor-pointer px-5 py-2 bg-white/20 hover:bg-white/30 backdrop-blur rounded-3xl transition-all">Catering</div>
                <div onclick="quickSearch('Photography')" class="cursor-pointer px-5 py-2 bg-white/20 hover:bg-white/30 backdrop-blur rounded-3xl transition-all">Photography</div>
                <div onclick="quickSearch('DJ')" class="cursor-pointer px-5 py-2 bg-white/20 hover:bg-white/30 backdrop-blur rounded-3xl transition-all">DJ</div>
                <div onclick="quickSearch('Ghanaian')" class="cursor-pointer px-5 py-2 bg-white/20 hover:bg-white/30 backdrop-blur rounded-3xl transition-all">Ghanaian</div>
            </div>
        </div>
    </header>

    <!-- Search Results (hidden until a search runs) -->
    <section id="results-section" class="hidden py-16 bg-white scroll-mt-20">
        <div class="max-w-7xl mx-auto px-6">
            <div class="flex justify-between items-end mb-8">
                <div>
                    <h2 class="text-3xl font-bold heading-font">Search Results</h2>
                    <p id="results-count" class="text-gray-600"></p>
                </div>
                <button onclick="clearSearch()" class="text-brand hover:text-brand-700 font-medium">Clear</button>
            </div>
            <div class="grid md:grid-cols-3 lg:grid-cols-4 gap-8" id="results-grid"></div>
        </div>
    </section>

    <!-- Categories -->
    <section id="categories-section" class="py-16 bg-white scroll-mt-20">
        <div class="max-w-7xl mx-auto px-6">
            <h2 class="text-4xl font-bold text-center mb-4 heading-font">Popular Categories</h2>
            <p class="text-center text-gray-600 mb-12">Discover vendors who understand your traditions</p>
            <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-6" id="categories-grid"></div>
        </div>
    </section>

    <!-- Featured Vendors -->
    <section class="py-16 bg-gray-50">
        <div class="max-w-7xl mx-auto px-6">
            <div class="flex justify-between items-end mb-10">
                <div>
                    <h2 class="text-4xl font-bold heading-font">Featured Vendors</h2>
                    <p class="text-gray-600">Highly rated in your community</p>
                </div>
                <button onclick="scrollToSearch()" class="text-brand hover:text-brand-700 font-medium flex items-center gap-2">See all <i class="fa-solid fa-arrow-right"></i></button>
            </div>
            <div class="grid md:grid-cols-3 lg:grid-cols-4 gap-8" id="featured-vendors">
                <div class="col-span-full text-center text-gray-400 py-10">Loading vendors…</div>
            </div>
        </div>
    </section>

    <!-- How it Works -->
    <section id="how-section" class="py-20 bg-white scroll-mt-20">
        <div class="max-w-5xl mx-auto px-6 text-center">
            <h2 class="text-4xl font-bold mb-4 heading-font">How Vendor2U Works</h2>
            <div class="grid md:grid-cols-3 gap-10 mt-16">
                <div><div class="w-20 h-20 mx-auto bg-brand-50 text-brand rounded-2xl flex items-center justify-center text-4xl mb-6">1</div><h3 class="font-semibold text-xl mb-3">Search &amp; Discover</h3><p class="text-gray-600">Find vendors by location, culture, and service.</p></div>
                <div><div class="w-20 h-20 mx-auto bg-brand-50 text-brand rounded-2xl flex items-center justify-center text-4xl mb-6">2</div><h3 class="font-semibold text-xl mb-3">Compare &amp; Connect</h3><p class="text-gray-600">Request quotes and compare packages.</p></div>
                <div><div class="w-20 h-20 mx-auto bg-brand-50 text-brand rounded-2xl flex items-center justify-center text-4xl mb-6">3</div><h3 class="font-semibold text-xl mb-3">Book Securely</h3><p class="text-gray-600">Book with confidence and protection.</p></div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-navy-900 text-gray-400 py-16">
        <div class="max-w-7xl mx-auto px-6 grid md:grid-cols-3 gap-10">
            <div>
                <div class="flex items-center gap-3 text-white mb-6">
                    <div class="w-11 h-11 bg-white rounded-xl flex items-center justify-center text-xl font-extrabold tracking-tight text-navy">V<span class="text-brand">2</span>U</div>
                    <span class="text-3xl font-bold">Vendor<span class="text-brand">2</span>U</span>
                </div>
                <p class="text-sm">Your event. Your culture. Your vendors.</p>
            </div>
            <div>
                <h4 class="font-semibold text-white mb-4">Platform</h4>
                <ul class="space-y-2 text-sm">
                    <li><a href="#" onclick="scrollToSearch();return false;" class="hover:text-white">Browse Vendors</a></li>
                    <li><a href="#" onclick="showBecomeVendor();return false;" class="hover:text-white">For Vendors</a></li>
                </ul>
            </div>
            <div>
                <h4 class="font-semibold text-white mb-4">Follow Us</h4>
                <div class="flex gap-4 text-2xl">
                    <i class="fa-brands fa-instagram hover:text-white cursor-pointer"></i>
                    <i class="fa-brands fa-facebook hover:text-white cursor-pointer"></i>
                    <i class="fa-brands fa-x-twitter hover:text-white cursor-pointer"></i>
                </div>
            </div>
        </div>
        <div class="text-center text-xs mt-16 border-t border-gray-800 pt-8">© 2026 Vendor2U.com</div>
    </footer>

    <!-- Login Modal -->
    <div id="login-modal" class="hidden fixed inset-0 bg-black/70 flex items-center justify-center z-[100] p-4">
        <div class="bg-white rounded-3xl max-w-md w-full overflow-hidden">
            <div class="p-8">
                <h2 class="text-3xl font-semibold mb-8 text-center">Welcome back</h2>
                <div class="space-y-4">
                    <button onclick="fakeLogin('customer')" class="w-full py-4 border border-gray-300 hover:bg-gray-50 rounded-2xl font-medium">Continue as Customer</button>
                    <button onclick="fakeLogin('vendor')" class="w-full py-4 border border-gray-300 hover:bg-gray-50 rounded-2xl font-medium">Continue as Vendor</button>
                </div>
                <p class="text-center text-xs text-gray-400 mt-6">Sign-in is a placeholder for now — accounts come later.</p>
            </div>
            <div onclick="toggleLoginModal()" class="border-t py-4 text-center text-sm cursor-pointer hover:bg-gray-50">Close</div>
        </div>
    </div>

    <!-- Modal mount point (vendor profile, become-vendor, quote) -->
    <div id="modal-root"></div>

    <!-- Toast -->
    <div id="toast" class="hidden fixed bottom-6 right-6 bg-gray-900 text-white px-6 py-4 rounded-2xl shadow-2xl z-[300] max-w-sm"></div>

    <script>
    // ---------- Small helpers ----------
    const $ = (id) => document.getElementById(id);
    const esc = (s) => String(s ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

    function toast(msg) {
        const t = $('toast');
        t.textContent = msg;
        t.classList.remove('hidden');
        clearTimeout(window._toastTimer);
        window._toastTimer = setTimeout(() => t.classList.add('hidden'), 3500);
    }
    function money(v) { const n = Number(v.priceRange.min); return n ? `From $${n.toLocaleString()} ${v.priceRange.unit || ''}`.trim() : 'Contact for pricing'; }

    // ---------- Load featured vendors from the database ----------
    async function loadFeatured() {
        try {
            const res = await fetch('/api/vendors');
            const vendors = await res.json();
            const box = $('featured-vendors');
            box.innerHTML = '';
            vendors.slice(0, 4).forEach(v => box.appendChild(vendorCard(v)));
            if (!vendors.length) box.innerHTML = '<div class="col-span-full text-center text-gray-400 py-10">No vendors yet.</div>';
        } catch (e) {
            $('featured-vendors').innerHTML = '<div class="col-span-full text-center text-red-400 py-10">Could not load vendors. Is the server running?</div>';
        }
    }

    // ---------- Build a vendor card element ----------
    function vendorCard(v) {
        const card = document.createElement('div');
        card.className = "bg-white rounded-3xl overflow-hidden border border-gray-100 hover:border-brand/30 transition-all cursor-pointer shadow-sm hover:shadow-xl";
        card.innerHTML = `
            <img src="${esc(v.image)}" alt="${esc(v.name)}" class="w-full h-48 object-cover">
            <div class="p-6">
                <div class="flex justify-between items-start">
                    <div>
                        <div class="font-semibold text-lg">${esc(v.name)}</div>
                        <div class="text-sm text-gray-500">${esc(v.location.city)}, ${esc(v.location.state)}</div>
                    </div>
                    <div class="text-right">
                        <div class="text-brand font-bold">★ ${v.rating}</div>
                        <div class="text-xs text-gray-500">${v.reviews} reviews</div>
                    </div>
                </div>
                <div class="mt-3 flex flex-wrap gap-1">
                    ${v.culturalSpecialties.slice(0,2).map(c=>`<span class="text-xs bg-brand-50 text-brand-700 px-2 py-1 rounded-lg">${esc(c)}</span>`).join('')}
                    ${v.verified ? '<span class="text-xs bg-green-50 text-green-700 px-2 py-1 rounded-lg">✓ Verified</span>' : ''}
                </div>
                <div class="mt-4 text-brand font-medium">${esc(money(v))}</div>
            </div>`;
        card.onclick = () => showVendorProfile(v);
        return card;
    }

    // ---------- Search (calls the real API) ----------
    async function performSearch() {
        const service = $('search-input').value.trim();
        const location = $('location-input').value.trim();
        const params = new URLSearchParams();
        if (service) params.set('q', service);
        if (location) params.set('q', service ? service : location); // simple combined text search
        // Prefer a combined text query so either box works:
        const q = [service, location].filter(Boolean).join(' ');
        const url = q ? `/api/vendors?q=${encodeURIComponent(service || location)}` : '/api/vendors';

        try {
            const res = await fetch(url);
            let vendors = await res.json();
            // If both boxes filled, further filter by location text on the client
            if (service && location) {
                vendors = vendors.filter(v =>
                    (v.location.city + ' ' + v.location.state).toLowerCase().includes(location.toLowerCase()));
            }
            renderResults(vendors, q);
        } catch (e) {
            toast('Search failed. Is the server running?');
        }
    }

    function renderResults(vendors, query) {
        const sec = $('results-section');
        const grid = $('results-grid');
        grid.innerHTML = '';
        $('results-count').textContent = `${vendors.length} vendor${vendors.length===1?'':'s'} found${query?` for “${query}”`:''}`;
        if (!vendors.length) {
            grid.innerHTML = '<div class="col-span-full text-center text-gray-500 py-12">No matches. Try a broader search like “Catering” or a state like “TX”.</div>';
        } else {
            vendors.forEach(v => grid.appendChild(vendorCard(v)));
        }
        sec.classList.remove('hidden');
        sec.scrollIntoView({ behavior: 'smooth' });
    }

    function quickSearch(term) { $('search-input').value = term; $('location-input').value=''; performSearch(); }
    function clearSearch() { $('results-section').classList.add('hidden'); $('search-input').value=''; $('location-input').value=''; }
    function scrollToSearch() { window.scrollTo({top:0, behavior:'smooth'}); setTimeout(()=>$('search-input').focus(), 400); }
    function scrollToId(id) { $(id).scrollIntoView({behavior:'smooth'}); }
    function goHome() { window.scrollTo({top:0, behavior:'smooth'}); }

    // ---------- Categories ----------
    function initCategories() {
        const categories = [
            {name:"Catering", icon:"🍲"}, {name:"Photography", icon:"📸"},
            {name:"DJ", icon:"🎵"}, {name:"Decor", icon:"🌸"},
            {name:"Makeup", icon:"💄"}, {name:"Fashion", icon:"👗"}
        ];
        const grid = $('categories-grid');
        categories.forEach(cat => {
            const div = document.createElement('div');
            div.className = "bg-white border border-gray-200 hover:border-brand/40 p-6 rounded-3xl text-center cursor-pointer transition-all hover:shadow-md";
            div.innerHTML = `<div class="text-5xl mb-4">${cat.icon}</div><div class="font-semibold">${cat.name}</div>`;
            div.onclick = () => quickSearch(cat.name);
            grid.appendChild(div);
        });
    }

    // ---------- Vendor profile modal ----------
    function showVendorProfile(v) {
        const html = `
        <div class="fixed inset-0 bg-black/80 flex items-center justify-center z-[200] p-4" onclick="if(event.target===this)this.remove()">
            <div class="bg-white max-w-2xl w-full rounded-3xl overflow-hidden max-h-[90vh] overflow-y-auto">
                <img src="${esc(v.image)}" alt="${esc(v.name)}" class="w-full h-72 object-cover">
                <div class="p-8">
                    <div class="flex justify-between items-start gap-4">
                        <div>
                            <h2 class="text-3xl font-bold heading-font">${esc(v.name)}</h2>
                            <p class="text-gray-600">${esc(v.location.city)}, ${esc(v.location.state)} ${v.verified?'• <span class="text-green-600">Verified</span>':''}</p>
                        </div>
                        <div class="text-right shrink-0">
                            <div class="text-3xl font-bold text-brand">★ ${v.rating}</div>
                            <div class="text-sm text-gray-500">${v.reviews} reviews</div>
                        </div>
                    </div>
                    <p class="mt-6 text-gray-700 leading-relaxed">${esc(v.bio || '')}</p>
                    <div class="mt-6 flex flex-wrap gap-2">
                        ${v.culturalSpecialties.map(c=>`<span class="text-sm bg-brand-50 text-brand-700 px-3 py-1 rounded-xl">${esc(c)}</span>`).join('')}
                    </div>
                    <div class="mt-6 grid grid-cols-2 gap-4 text-sm bg-gray-50 rounded-2xl p-4">
                        <div><span class="text-gray-500">Category</span><br><b>${esc(v.category)}</b></div>
                        <div><span class="text-gray-500">Pricing</span><br><b>${esc(money(v))}</b></div>
                        <div><span class="text-gray-500">Experience</span><br><b>${v.yearsInBusiness} years</b></div>
                        <div><span class="text-gray-500">Serves within</span><br><b>${v.serviceRadius} miles</b></div>
                    </div>
                    <button onclick="showQuoteForm('${v.id}', ${JSON.stringify(v.name).replace(/"/g,'&quot;')})" class="w-full mt-8 py-4 bg-brand hover:bg-brand-700 text-white rounded-2xl font-semibold text-lg">Request a Quote</button>
                    <button onclick="this.closest('.fixed').remove()" class="w-full mt-3 py-4 text-gray-600 border border-gray-300 rounded-2xl hover:bg-gray-50">Close</button>
                </div>
            </div>
        </div>`;
        mountModal(html);
    }

    // ---------- Quote request form (saves to database) ----------
    function showQuoteForm(vendorId, vendorName) {
        document.querySelectorAll('#modal-root .fixed').forEach(m=>m.remove());
        const html = `
        <div class="fixed inset-0 bg-black/80 flex items-center justify-center z-[200] p-4" onclick="if(event.target===this)this.remove()">
            <div class="bg-white max-w-md w-full rounded-3xl overflow-hidden max-h-[90vh] overflow-y-auto">
                <div class="p-8">
                    <h2 class="text-2xl font-bold mb-1">Request a Quote</h2>
                    <p class="text-gray-500 mb-6">to ${esc(vendorName)}</p>
                    <form onsubmit="submitQuote(event, '${vendorId}')" class="space-y-4">
                        <input required name="customerName" placeholder="Your name" class="w-full px-4 py-3 border rounded-2xl focus:outline-none focus:ring-2 focus:ring-brand">
                        <input required type="email" name="customerEmail" placeholder="Your email" class="w-full px-4 py-3 border rounded-2xl focus:outline-none focus:ring-2 focus:ring-brand">
                        <input name="eventType" placeholder="Event type (e.g. Wedding)" class="w-full px-4 py-3 border rounded-2xl focus:outline-none focus:ring-2 focus:ring-brand">
                        <input type="date" name="eventDate" class="w-full px-4 py-3 border rounded-2xl focus:outline-none focus:ring-2 focus:ring-brand text-gray-600">
                        <textarea name="message" rows="3" placeholder="Tell them about your event…" class="w-full px-4 py-3 border rounded-2xl focus:outline-none focus:ring-2 focus:ring-brand"></textarea>
                        <button type="submit" class="w-full py-4 bg-brand hover:bg-brand-700 text-white rounded-2xl font-semibold">Send Request</button>
                        <button type="button" onclick="this.closest('.fixed').remove()" class="w-full py-3 text-gray-600">Cancel</button>
                    </form>
                </div>
            </div>
        </div>`;
        mountModal(html);
    }

    async function submitQuote(e, vendorId) {
        e.preventDefault();
        const f = e.target;
        const payload = {
            vendorId,
            customerName: f.customerName.value,
            customerEmail: f.customerEmail.value,
            eventType: f.eventType.value,
            eventDate: f.eventDate.value,
            message: f.message.value
        };
        try {
            const res = await fetch('/api/bookings', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload) });
            if (!res.ok) throw new Error();
            document.querySelectorAll('#modal-root .fixed').forEach(m=>m.remove());
            toast('Quote request sent and saved ✓');
        } catch (err) { toast('Could not send. Please try again.'); }
    }

    // ---------- Become a Vendor form (saves to database) ----------
    function showBecomeVendor() {
        const html = `
        <div class="fixed inset-0 bg-black/80 flex items-center justify-center z-[200] p-4" onclick="if(event.target===this)this.remove()">
            <div class="bg-white max-w-lg w-full rounded-3xl overflow-hidden max-h-[90vh] overflow-y-auto">
                <div class="p-8">
                    <h2 class="text-2xl font-bold mb-1">Become a Vendor</h2>
                    <p class="text-gray-500 mb-6">List your business on Vendor2U. You'll appear as unverified until reviewed.</p>
                    <form onsubmit="submitVendor(event)" class="space-y-4">
                        <input required name="name" placeholder="Business name *" class="w-full px-4 py-3 border rounded-2xl focus:outline-none focus:ring-2 focus:ring-brand">
                        <input name="owner" placeholder="Your name" class="w-full px-4 py-3 border rounded-2xl focus:outline-none focus:ring-2 focus:ring-brand">
                        <select required name="category" class="w-full px-4 py-3 border rounded-2xl text-gray-700 focus:outline-none focus:ring-2 focus:ring-brand">
                            <option value="">Select a category *</option>
                            <option>Catering</option><option>Photography</option><option>DJ</option>
                            <option>Decor</option><option>Makeup</option><option>Fashion</option>
                        </select>
                        <div class="grid grid-cols-2 gap-3">
                            <input name="city" placeholder="City" class="px-4 py-3 border rounded-2xl focus:outline-none focus:ring-2 focus:ring-brand">
                            <input name="state" placeholder="State (e.g. NJ)" maxlength="2" class="px-4 py-3 border rounded-2xl focus:outline-none focus:ring-2 focus:ring-brand uppercase">
                        </div>
                        <div class="grid grid-cols-2 gap-3">
                            <input name="priceMin" type="number" placeholder="Starting price ($)" class="px-4 py-3 border rounded-2xl focus:outline-none focus:ring-2 focus:ring-brand">
                            <input name="cultural" placeholder="Cultures (comma sep.)" class="px-4 py-3 border rounded-2xl focus:outline-none focus:ring-2 focus:ring-brand">
                        </div>
                        <textarea name="bio" rows="3" placeholder="Short description of your services…" class="w-full px-4 py-3 border rounded-2xl focus:outline-none focus:ring-2 focus:ring-brand"></textarea>
                        <button type="submit" class="w-full py-4 bg-brand hover:bg-brand-700 text-white rounded-2xl font-semibold">Submit Listing</button>
                        <button type="button" onclick="this.closest('.fixed').remove()" class="w-full py-3 text-gray-600">Cancel</button>
                    </form>
                </div>
            </div>
        </div>`;
        mountModal(html);
    }

    async function submitVendor(e) {
        e.preventDefault();
        const f = e.target;
        const payload = {
            name: f.name.value, owner: f.owner.value, category: f.category.value,
            city: f.city.value, state: f.state.value.toUpperCase(),
            priceMin: f.priceMin.value,
            culturalSpecialties: f.cultural.value.split(',').map(s=>s.trim()).filter(Boolean),
            bio: f.bio.value
        };
        try {
            const res = await fetch('/api/vendors', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload) });
            if (!res.ok) throw new Error();
            document.querySelectorAll('#modal-root .fixed').forEach(m=>m.remove());
            toast('Your listing was submitted ✓');
            loadFeatured();
        } catch (err) { toast('Could not submit. Check required fields.'); }
    }

    // ---------- Modal + login utilities ----------
    function mountModal(html) {
        const wrap = document.createElement('div');
        wrap.innerHTML = html;
        $('modal-root').appendChild(wrap.firstElementChild);
    }
    function toggleLoginModal() { $('login-modal').classList.toggle('hidden'); }
    function fakeLogin(role) { toggleLoginModal(); toast(`Signed in as ${role} (demo)`); }

    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') {
            const modals = document.querySelectorAll('#modal-root .fixed');
            if (modals.length) modals[modals.length-1].remove();
            else $('login-modal').classList.add('hidden');
        }
    });

    // ---------- Start ----------
    window.onload = () => { initCategories(); loadFeatured(); };
    </script>
</body>
</html>
