services:
  - type: web
    name: vendor2u
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DB_PATH
        value: /tmp/vendor2u.db   # using /tmp (ephemeral but works on free tier)
